# Session Report — Session-20260925-AuthMeAnonymousProbe

**فاز:** رفع نقص پس از فاز ۸ (بدون تغییر فاز)
**تاریخ:** `2026-09-25`
**دستور کاربر:** «خطا دارم» + گزارش کنسول مرورگر: `GET http://localhost:6300/api/v1/auth/me 401 (Unauthorized)`

---

## ۱. خلاصه Session
خطای گزارش‌شده یک باگ کارکردی نبود؛ «بررسی نشست» (`/api/v1/auth/me`) برای بازدیدکنندهٔ واردنشده با ۴۰۱ پاسخ می‌داد و این خطا در کنسول مرورگر به‌عنوان خطا دیده می‌شد. چون نبودِ نشست یک وضعیت عادی است، قرارداد این Endpoint به `204 No Content` تغییر کرد و کوکی نشست کهنه نیز پاک می‌شود؛ سایر Endpointها رفتار fail-closed (۴۰۱) خود را حفظ کردند.

## ۲. ریشه‌یابی
- `AuthController.Me` با `[Authorize]` محافظت شده بود.
- `BffSessionAuthenticationHandler` در نبود/بی‌اعتباری نشست، `AuthenticateResult.Fail("نشست معتبر نیست.")` می‌داد ⇒ پاسخ ۴۰۱.
- فرانت‌اند این ۴۰۱ را «مدیریت» می‌کرد (`useMe` بدون retry روی ۴۰۱، `RequireAuth` و صفحهٔ ورود)، اما خطای شبکه در کنسول مرورگر باقی می‌ماند و گمراه‌کننده بود.
- نکته مهم: `http://localhost:6300` (dev server فرانت) با rewrite به بک‌اند (`http://localhost:5197`) وصل می‌شود؛ بنابراین پیام ۴۰۱ از بک‌اند بود، نه پروکسی.

## ۳. تغییرات
| فایل | تغییر |
|---|---|
| `Backend/src/.../API/Controllers/AuthController.cs` | `[Authorize]` → `[AllowAnonymous]`؛ بازگشت `204 No Content` برای ناشناس؛ پاک‌کردن کوکی نشست کهنه؛ مستند XML |
| `Frontend/src/lib/api/auth.ts` | `fetchMe` با تایپ `CurrentUser \| null` (۲۰۴ ⇒ `null`) |

## ۴. اعتبارسنجی
| مورد | نتیجه |
|---|---|
| بیلد بک‌اند (`dotnet build OrganizationalStructure.slnx`) | ✅ `Build succeeded` — ۰ هشدار/۰ خطا |
| تست‌ها (۵ پروژه) | ✅ ۲۴۷ سبز (۳۷ + ۹۸ + ۲۷ + ۴ + ۸۱) |
| فرانت‌اند | ✅ `tsc --noEmit` · `eslint` · `next build` |
| تست زنده بک‌اند (بدون کوکی) | ✅ `/api/v1/auth/me` = **۲۰۴** · `/api/v1/posts` = **۴۰۱** |

> برای اجرای تست‌ها، نمونهٔ در حال اجرای بک‌اند (که فایل‌های `bin` را قفل کرده بود) متوقف شد؛ کاربر باید بک‌اند را دوباره اجرا کند تا اصلاح فعال شود.

## ۵. تصمیم‌ها
- **DEC-036:** `/api/v1/auth/me` = بررسی نشست؛ ناشناس ⇒ `204` (به‌جای ۴۰۱)؛ سایر Endpointها ۴۰۱ fail-closed. DEC-033 (۴۰۱ برای ورود ناموفق) نقض نشد.

## ۶. نکات باقی‌مانده
- تست یکپارچگی برای حالت «ناشناس» اضافه نشد چون `TestWebApplicationFactory` همهٔ درخواست‌ها را با `TestAuthHandler` احراز می‌کند (امکان شبیه‌سازی ناشناس در سطح API وجود ندارد)؛ به‌جای آن، رفتار با اجرای واقعی بک‌اند تأیید شد.
- نشست‌ها در توسعه درون‌حافظه‌ای‌اند (`InMemoryBffSessionStore`)؛ با هر ری‌استارت بک‌اند، کاربر باید دوباره وارد شود (در Production با Redis — فاز ۸ — رفع می‌شود).